<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>DesiLinkr Monthly Report</title>
  <style>
    body {
      font-family: "Segoe UI", Arial, sans-serif;
      background-color: #f3f4f6;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 700px;
      margin: 40px auto;
      background: #fff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 4px 14px rgba(0,0,0,0.08);
    }
    .header {
      display: flex;
      align-items: center;
      padding: 20px 30px;
      border-bottom: 1px solid #eee;
    }
    .header img {
      height: 40px;
      margin-right: 12px;
    }
    .header h1 {
      font-size: 20px;
      margin: 0;
      color: #111;
    }
    .summary {
      display: flex;
      justify-content: space-around;
      text-align: center;
      padding: 20px 0;
      background: #fafafa;
      border-bottom: 1px solid #eee;
    }
    .stat {
      flex: 1;
    }
    .stat h2 {
      font-size: 22px;
      margin: 0;
      color: #2563eb;
    }
    .stat p {
      font-size: 13px;
      margin: 4px 0 0;
      color: #555;
    }
    .content {
      padding: 25px 30px;
    }
    .content h3 {
      font-size: 16px;
      margin: 0 0 12px;
      color: #111;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 25px;
    }
    th, td {
      text-align: left;
      padding: 10px;
      font-size: 13px;
    }
    th {
      background: #f9fafb;
      font-weight: 600;
      border-bottom: 1px solid #eee;
    }
    tr:nth-child(even) {
      background: #fafafa;
    }
    .footer {
      text-align: center;
      padding: 15px;
      font-size: 12px;
      color: #888;
      border-top: 1px solid #eee;
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- Header -->
    <div class="header">
      <img src="{{logoUrl}}" alt="DesiLinkr Logo"/>
      <h1>Monthly Report - {{month}}</h1>
    </div>

    <!-- Summary -->
    <div class="summary">
      <div class="stat">
        <h2>{{totalClicks}}</h2>
        <p>Total Clicks</p>
      </div>
      <div class="stat">
        <h2>{{totalQrScans}}</h2>
        <p>Total QR Scans</p>
      </div>
      <div class="stat">
        <h2>{{newLinks}}</h2>
        <p>New Links Created This Month</p>
      </div>
    </div>

    <!-- Content -->
    <div class="content">
      <!-- Top Links -->
      <h3>🔗 Top 3 Links This Month</h3>
      <table>
        <tr>
          <th>Title</th>
          <th>URL</th>
          <th>Clicks</th>
          <th>Created On</th>
        </tr>
        {{#each topLinks}}
        <tr>
          <td>{{title}}</td>
          <td><a href="{{url}}">{{shortUrl}}</a></td>
          <td>{{clicks}}</td>
          <td>{{createdOn}}</td>
        </tr>
        {{/each}}
      </table>

      <!-- Top QR Codes -->
      <h3>📱 Top 3 QR Codes This Month</h3>
      <table>
        <tr>
          <th>QR Name</th>
          <th>Linked URL</th>
          <th>Scans</th>
          <th>Created On</th>
        </tr>
        {{#each topQRCodes}}
        <tr>
          <td>{{name}}</td>
          <td><a href="{{url}}">{{shortUrl}}</a></td>
          <td>{{scans}}</td>
          <td>{{createdOn}}</td>
        </tr>
        {{/each}}
      </table>

      <!-- Traffic by Country -->
      <h2 style="font-size:18px; color:#333;">🌍 Traffic by Country</h2>
      <p style="font-size:14px; color:#666;">Here are the top countries driving traffic this month:</p>

      <table width="100%" cellpadding="0" cellspacing="0" style="border-collapse:collapse; margin-top:10px; font-size:14px; color:#444;">
        <thead>
          <tr style="background:#f5f5f5; text-align:left;">
            <th style="padding:8px; border:1px solid #ddd;">Rank</th>
            <th style="padding:8px; border:1px solid #ddd;">Country</th>
            <th style="padding:8px; border:1px solid #ddd;">Clicks</th>
            <th style="padding:8px; border:1px solid #ddd;">QR Scans</th>
          </tr>
        </thead>
        <tbody>
          {{#each countries}}
          <tr>
            <td style="padding:8px; border:1px solid #ddd;">{{rank}}</td>
            <td style="padding:8px; border:1px solid #ddd;">{{flag}} {{name}}</td>
            <td style="padding:8px; border:1px solid #ddd;">{{clicks}}</td>
            <td style="padding:8px; border:1px solid #ddd;">{{qrScans}}</td>
          </tr>
          {{/each}}
          <tr style="background:#fafafa; font-weight:bold;">
            <td style="padding:8px; border:1px solid #ddd;">—</td>
            <td style="padding:8px; border:1px solid #ddd;">🌐 Other Countries</td>
            <td style="padding:8px; border:1px solid #ddd;">{{otherCountries.clicks}}</td>
            <td style="padding:8px; border:1px solid #ddd;">{{otherCountries.qrScans}}</td>
          </tr>
        </tbody>
      </table>
    </div>

    <!-- Footer -->
    <div class="footer">
      © {{year}} DesiLinkr Reports · All rights reserved
    </div>
  </div>
</body>
</html>
